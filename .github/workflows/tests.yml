name: tests

on: [pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Script
        run: sleep 15s 
      - run: exit 1
  message:
    if: github.base_ref == 'master'
    runs-on: ubuntu-latest
    steps:
      - run: echo failure! 